apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flowworld.fullname" . }}
  labels:
    {{- include "flowworld.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
