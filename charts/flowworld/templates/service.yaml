apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowworld.fullname" . }}
  labels:
    {{- include "flowworld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flowworld.selectorLabels" . | nindent 4 }}
