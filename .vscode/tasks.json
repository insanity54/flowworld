{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Pocketbase",
      "type": "shell",
      "command": "set -a; . .env.development.local; go run . serve --http localhost:$PORT --dev --dir ./pb_data",
      "problemMatcher": [],
      "isBackground": true,
      "options": {
        "cwd": "${workspaceFolder}",
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}